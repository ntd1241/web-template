Bạn là kỹ sư triển khai trên repo này (Metronic 9 React + TS, Vite, React Router 7, RHF + zod, Tailwind 4). Toàn bộ công việc nằm trong src/examples/material/** (mock-first, example-only).

ĐỌC TRƯỚC KHI CODE:
1. docs/superpowers/plans/2026-06-26-inspection-checklists-and-palang-mock.md  (KẾ HOẠCH — làm tuần tự Task 1 → 7)
2. docs/superpowers/specs/2026-06-26-inspection-checklists-and-palang-mock-design.md  (spec chi tiết)
3. AGENTS.md (mục "Builder fast path" + "UI Implementation Rules")

NHIỆM VỤ (tóm tắt):
- Thêm domain "Bảng kiểm định" (InspectionTable + InspectionCriterion) và trang CRUD master-detail: trái = list bảng kiểm định, phải = sửa thông tin bảng + checklist tiêu chí (thêm/sửa/xóa từng tiêu chí). Thêm route + menu "Bảng kiểm định".
- Form mẫu vật tư: thêm switch "Quản lý an toàn"; bật thì hiện select chọn bảng kiểm định + preview checklist tiêu chí.
- Trang Mẫu vật tư: thêm cây nhóm bên trái; click nhóm → lọc bảng mẫu (gồm nhóm con). Có nút "Tất cả".
- Trang tạo/sửa vật tư: thêm select "Nhóm" TRƯỚC select "Mẫu"; chọn nhóm → select Mẫu chỉ còn mẫu thuộc nhóm đó (gồm nhóm con); đổi nhóm thì clear Mẫu.
- Mock từ Palang.xlsx: nhóm "Vật tư kiểm định" › "Palăng" (chèn đầu mảng nhóm); value set Mức tải (3 mức) + 6 spec-definition palăng; mẫu "Palăng xích tay" (isSafetyManaged + inspectionTableId='insp-palang', chèn đầu mảng mẫu); 5 thiết bị palăng thật (chèn đầu mảng vật tư). Bảng kiểm định palăng 9 tiêu chí copy nguyên văn từ sheet 2.

QUY TẮC BẮT BUỘC:
- Tiếng Việt cho mọi label/placeholder/validation/empty/confirm.
- Form sinh được phải đi qua form-builder: sửa *.form.fixture.ts rồi chạy
    npm run gen:form -- <fixture-path> <out-path>
  rồi mới wire submit/reset ở parent. KHÔNG sửa banner generated. File đã có chỉnh tay (DeviceSpecField, preview...) thì gen ra /tmp scratch rồi reconcile, đừng đè mất.
- Named exports cho component/page mới; handler handle*; boolean is/has/can; import type; alias @/ cho cross-folder. Không thêm any.
- Tái dùng src/components/ui và component sẵn có (MaterialGroupTree, ConfirmDeleteDialog, DataGrid). Không tự dựng lại button/input/table thô.
- Không sửa trang public-detail. Không refactor enum legacy MaterialGroupKey.

QUY TRÌNH:
- Làm từng Task theo plan; mỗi Task chạy `npm run build` (và `npm run test:run` khi plan yêu cầu) cho PASS rồi commit với message như plan ghi.
- Có test trong plan (Task 5) thì làm TDD: viết test → chạy fail → implement → pass.
- Nếu menu.config.test.ts assert số item/path, cập nhật test cho khớp item menu mới.
- Lưu ý 3 rủi ro plan đã nêu: chữ ký getSelfAndDescendantIds; cách materialModelFormSchema đang khai báo (chèn refine đúng chỗ); menu.config.test.ts.

KẾT THÚC: chạy `npm run build` + `npm run test:run` đảm bảo xanh. KHÔNG push — để người review chạy build và review diff.
